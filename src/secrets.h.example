// Copy this file to src/secrets.h (git-ignored) and fill in real values.
// secrets.h is never committed — it contains live WiFi and MQTT credentials.
#pragma once

namespace secrets {

constexpr const char* kWifiSsid       = "your-ssid";
constexpr const char* kWifiPassword   = "your-pass";
constexpr const char* kMqttHost       = "your-ha-host";
constexpr int         kMqttPort       = 8883;  // TLS MQTT standard port
constexpr const char* kMqttUser       = "your-mqtt-user";
constexpr const char* kMqttPassword   = "your-mqtt-pass";
constexpr const char* kMqttClientId   = "gps_skyview_esp32";

// PEM-encoded CA certificate for broker TLS verification.
// Paste the full certificate chain from your broker's CA here.
constexpr const char* kMqttCaCert = R"EOF(
-----BEGIN CERTIFICATE-----
...broker CA here...
-----END CERTIFICATE-----
)EOF";

}  // namespace secrets
